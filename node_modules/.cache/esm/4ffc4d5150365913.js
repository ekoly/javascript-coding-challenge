"main";_503‍.x([["html",()=>html]]);// @ts-check

       function html(tmplArr, ...args) {
  const d = tmplArr.map((n, i) => args[i] == null ? n : `${n}${args[i]}`).join('');
  return `<!doctype html>${d}`;
}

_503‍.d(html);